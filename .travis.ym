language: node_js
cache:
  directories:
    - assets/vendor
install:
  - npm install -g bower
  - bower install
deploy:
  provider: s3
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY
  bucket: parenthesis.io
notifications:
    email:
        on_failure: always
